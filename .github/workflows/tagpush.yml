# File managed by web3-bot. DO NOT EDIT.
# See https://github.com/protocol/.github/ for details.

name: Tag Push Checker
on:
  push:
    tags:
      - v*

jobs:
  build:
    runs-on: [self-hosted, linux, tesseract]
    steps:
      - uses: actions/checkout@v2
      - name: test
        run: touch /tmp/sbtest
